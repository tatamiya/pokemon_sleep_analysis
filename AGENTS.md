# AGENTS.md

このファイルは、このプロジェクトを扱うAIエージェント向けの作業ルールを定める。

## 作業開始時

ファイルを編集する前に、原則として以下を確認する。

1. `README.md`
2. `ANALYSIS_SUMMARY.md`
3. 作業対象に対応する `analysis/` 以下のファイル
4. 記事を扱う場合は、対応する `article/` 以下のファイル

プロジェクトを開いた直後に、内容を十分確認せず編集を開始しないこと。

まず現在の分析内容、前提、ファイル構成を把握すること。

## 各ファイルの役割

### `analysis/*.qmd`

分析の一次資料。

統計計算、可視化、モデル推定、検定、数値結果は原則としてここで実装する。

### `article/*.md`

読者向けの記事草稿。

対応する分析ファイルから得られた結果を説明するためのファイルであり、
分析によって裏付けられていない数値や結論を追加しないこと。

### `ANALYSIS_SUMMARY.md`

現在の分析結果と解釈を簡潔にまとめたファイル。

プロジェクト全体を把握するための入口として使用する。

ただし、データ更新後に内容が古くなっている可能性があるため、
正確な数値が重要な場合は必ず最新の `.qmd` とレンダリング結果を確認すること。

### `analysis/archive/`

過去の探索的分析を保存する場所。

ここにある結果は最新データに基づいているとは限らない。

再実行または最新分析との照合をせずに、
現在の結果として扱わないこと。

明示的な指示がない限り、archive内のファイルは変更しないこと。

### `rendered/`

Quartoによる生成物。

このディレクトリ内のファイルを手作業で編集しないこと。

## 統計的な表現

統計的に正確な表現を使うこと。

例えば、

- 「帰無仮説は棄却されなかった」
- 「モデルと矛盾する結果は得られなかった」
- 「現在のデータはこのモデルと整合的である」

といった表現を優先する。

以下のような強すぎる表現には置き換えないこと。

- 「帰無仮説が正しいことが証明された」
- 「この確率であることが確認された」
- 「独立であることが証明された」

また、有意差が得られた場合も、
それだけで特定の対立モデルが正しいことを意味しない。

以下は異なる統計的な問いとして区別すること。

- 確率そのものの推定
- 適合度
- 2つの枠の独立性
- 時間に対する非定常性
- 過去の結果への依存性

## ベイズ推定の解釈

例えば、

\[
P(p_A < 1/3 \mid \mathrm{data})
\]

は、指定したモデルと事前分布のもとでの
パラメータに関する事後確率である。

これを、

「点帰無仮説 \(p_A=1/3\) が誤りである確率」

と解釈しないこと。

点帰無仮説と対立モデルをベイズ的に直接比較する場合は、
Bayes factor や点質量を含む事前分布など、
明示的なモデル比較が必要となる。

## 現在の通説モデル

現在検討している通説上のモデルは、

2枠目：

```text
A:B = 1:2
```

3枠目：

```text
A:B:C = 1:1:1
```

である。

さらに2枠目と3枠目が独立なら、

```text
AAA : AAB : AAC : ABA : ABB : ABC
 1  :  1  :  1  :  2  :  2  :  2
```

となる。

すなわち、

\[
P(AAA)=P(AAB)=P(AAC)=\frac19,
\]

\[
P(ABA)=P(ABB)=P(ABC)=\frac29.
\]

これらの正確な有理数値は、
公式情報によって確認されない限り、
「通説・コミュニティ上のモデル」として扱うこと。

公式仕様として断定しないこと。

## 分析対象データ

`df` と `df2` の違いに注意する。

### `df`

- 収集開始前のデータを除外
- 2枠種を除外
- 3枠目および6パターンの分析に使用

### `df2`

- 収集開始前のデータを除外
- 2枠種を含む
- 2枠目だけを分析する場合に使用

異なるサンプルから計算した統計量を、
同一の観測データに基づく結果であるかのように比較しないこと。

## 時系列分析

見た目だけから時間変化を確定しないこと。

現在のデータでは、
2枠目Aの出現率が初期には1/3付近に見え、
後半では低く見える。

しかし、これは現時点では探索的な観察である。

特に、

- グラフを見た後で「3月」を境界として選び、その前後比較を確証的検定として扱わない
- 滑らかな時間変化と急激な変化点を区別する
- 非定常性と過去の結果への依存性を区別する
- 複雑なモデルは固定確率モデルと比較する

こと。

「3月頃から低下しているように見える」ことを、
外部情報なしに
「3月にゲーム仕様が変更された」
と解釈しないこと。

## R / Quarto

既存コードの書き方に合わせ、
読みやすい tidyverse ベースのRコードを優先する。

既存オブジェクトを再利用できる場合は、
同じ計算を不用意に重複させない。

`.qmd` の本文にデータ依存の数値を書く場合は、
可能な限りインラインRを使用する。

特に以下は固定値として本文に直接書かない方がよい。

- サンプルサイズ
- 出現数
- 出現割合
- 検定統計量
- p値
- 信頼区間
- 信用区間

データ更新時に本文だけ古くなることを避けるためである。

ggplot2で線幅を指定する場合は、
非推奨の `size` ではなく `linewidth` を使用する。

各 `.qmd` は可能な範囲で独立して実行できるようにする。

少量のコード共有のためだけに、
別の `.qmd` に不要な依存関係を作らないこと。

## 記事執筆

記事は、
Pokémon Sleepを知っているが統計学の専門家とは限らない読者にも
理解できることを目指す。

検定やモデルを紹介する前に、
「何を知りたいのか」を説明する。

複数の分析結果が一見矛盾して見える場合は、
矛盾として処理するのではなく、
なぜ両立しうるのかを説明する。

探索的な発見と、
比較的強い統計的結論を明確に区別する。

外部の文献、公式仕様、コミュニティ上の通説などを
根拠なく生成しないこと。

外部情報が必要な場合は、
明示的に確認したうえで使用する。

## 編集方針

依頼された目的を達成するために必要な範囲だけ変更する。

以下を勝手に行わないこと。

- 関係のない節の全面的な書き換え
- 探索分析の削除
- ディレクトリ構成の大規模変更
- 生成済みHTMLの手編集
- 統計モデルや仮定の無断変更

依頼された変更によって分析結果の解釈が大きく変わる場合は、
変更前にその点を明示すること。

## Git

破壊的なGit操作を行わないこと。

明示的な指示がない限り、

- `reset --hard`
- history rewrite
- force push
- branch削除

などを行わない。

`.gitignore` に従い、
RStudioの状態ファイルやQuartoの生成物をGit管理に追加しないこと。